<CustomButton>:
    canvas.before:
        Rectangle:
            texture: self.current_texture
            size: self.size if self.current_texture else (0, 0)
            pos: self.pos[0], self.pos[1] - self.size[1] * 0.05

    valign: 'middle'
    halign: 'center'
